services:
  postgres:
    image: postgres:16
    container_name: samct-postgres
    environment:
      POSTGRES_DB: samctdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5433:5432"
    volumes:
      - samct_data:/var/lib/postgresql/data

volumes:
  samct_data: